import Link from "next/link";
import { MailCheck } from "lucide-react";
import { AuthCard } from "@/components/auth/auth-card";
export default async function VerifyEmailPage({searchParams}:{searchParams:Promise<{email?:string}>}){const {email}=await searchParams;return <AuthCard eyebrow="Verify your account" title="Check your email" description="Supabase accepted the registration request and requires email verification before Rowform can establish the account session."><div className="mt-6 rounded-2xl bg-[#eef6f2] p-5"><MailCheck className="text-[#16725e]"/><p className="mt-4 text-sm font-black">Verification required</p><p className="mt-2 text-sm leading-6 text-[#62706b]">Open the verification link sent to {email?<strong>{email}</strong>:"your registration address"}. The link returns securely to Rowform onboarding.</p></div><Link href="/sign-in" className="mt-6 inline-flex min-h-11 items-center font-black text-[#16725e] underline">Return to sign in</Link></AuthCard>}
