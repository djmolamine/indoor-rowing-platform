import "server-only";
import { createClient } from "@/lib/supabase/server";
import type { WorkoutRow } from "@/types/database";

export async function listCurrentAthleteWorkouts():Promise<WorkoutRow[]>{const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("AUTH_REQUIRED");const {data,error}=await supabase.from("workouts").select("*").eq("user_id",user.id).is("deleted_at",null).order("started_at",{ascending:false});if(error)throw new Error("WORKOUTS_UNAVAILABLE");return data??[];}
export async function createManualWorkout(input:{title:string;startedAt:string;distanceMeters?:number;durationMs?:number;providerKey?:string|null;modelKey?:string|null;machineClass?:string|null;notes?:string|null}){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("AUTH_REQUIRED");const pace=input.distanceMeters&&input.durationMs?Math.round(input.durationMs/(input.distanceMeters/500)):null;const {data,error}=await supabase.from("workouts").insert({user_id:user.id,title:input.title,started_at:input.startedAt,distance_meters:input.distanceMeters,duration_ms:input.durationMs,average_pace_ms_per_500m:pace,machine_provider_key:input.providerKey,machine_model_key:input.modelKey,machine_class:input.machineClass,source_method:"manual",verification_tier:"manual",workout_type:"fixed_distance",workout_role:"training",visibility:"private",ranking_eligible:false,notes:input.notes}).select("*").single();if(error)throw new Error("WORKOUT_CREATE_FAILED");return data;}
export async function softDeleteCurrentWorkout(id:string){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("AUTH_REQUIRED");const {error}=await supabase.from("workouts").update({deleted_at:new Date().toISOString()}).eq("id",id).eq("user_id",user.id);if(error)throw new Error("WORKOUT_DELETE_FAILED");}
